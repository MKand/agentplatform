[project]
name = "fun-fact-mcp-server"
version = "0.1.0"
description = "A simple MCP server for fun facts"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=0.1.0",
]

[project.scripts]
mcp-server = "server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
